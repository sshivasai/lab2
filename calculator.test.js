/*eslint-disable*/

const {
    add,
    subtract,
    multiply,
    divide,
    // squareRoot,
    // square,
  } = require('./calculator');
  
  describe('Calculator', () => {
    describe('add()', () => {
      // ... input the `add` the tests here
    });
  });
  